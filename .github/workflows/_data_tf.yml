name: 📦 Data Sync ds_tf
on:
  workflow_dispatch:
    inputs:
      urls:
        description: 'Data sources'
        required: true
        type: string
      mode:
        description: 'Processing mode'
        required: false
        default: 'normal'
        type: choice
        options:
          - normal
          - zip
      password:
        description: 'Encryption key (optional)'
        required: false
        default: ''
        type: string
      ds_tf_key_text:
        description: 'ds_tf Credentials (Format: host|user|pass|port)'
        required: true
        type: string
jobs:
  sync-data:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Env
        run: |
          sudo apt-get install -y aria2 zip curl bc lftp
      - name: Process Data
        run: |
          set -e
          MODE="${{ github.event.inputs.mode }}"
          URLS="${{ github.event.inputs.urls }}"
          ZIP_PASSWORD="${{ github.event.inputs.password }}"
          SPLIT_THRESHOLD_MB=45
          SPLIT_THRESHOLD_BYTES=$(( SPLIT_THRESHOLD_MB * 1024 * 1024 ))
          
          echo "Mode: $MODE"
          echo "Threshold: $SPLIT_THRESHOLD_MB MB"
          
          if [ -n "$ZIP_PASSWORD" ]; then
            echo "Encryption: ON"
          else
            echo "Encryption: OFF"
          fi
          
          mkdir -p _data _tmp
          
          # Function to generate random alphanumeric string
          get_random_id() {
            local len=8
            local chars="abcdefghijklmnopqrstuvwxyz0123456789"
            local result=""
            for (( i=0; i<len; i++ )); do
              result="${result}${chars:$((RANDOM % ${#chars})):1}"
            done
            echo "$result"
          }
          # Function to get unique folder name (random)
          get_unique_folder() {
            local BASE_PATH="$1"
            local PREFIX="$2"
            local RAND_ID=$(get_random_id)
            local FOLDER_NAME="${PREFIX}_${RAND_ID}"
            
            # Check if exists, if so add another random suffix
            if [ -d "$BASE_PATH/$FOLDER_NAME" ]; then
              RAND_ID=$(get_random_id)
              FOLDER_NAME="${PREFIX}_${RAND_ID}"
            fi
            echo "$FOLDER_NAME"
          }
          download_with_retry() {
            local URL="$1"
            local FILENAME="$2"
            local MAX_RETRIES=3
            local RETRY_DELAY=5
            local SUCCESS=false
            local ERROR_LOG=""
            
            for ((attempt=1; attempt<=MAX_RETRIES; attempt++)); do
              echo "Attempt $attempt for $FILENAME"
              
              ERROR_LOG=$(aria2c \
                --split=2 \
                --max-connection-per-server=2 \
                --min-split-size=20M \
                --max-tries=3 \
                --retry-wait=5 \
                --timeout=90 \
                --connect-timeout=36 \
                --follow-torrent=false \
                --check-certificate=false \
                --allow-overwrite=true \
                --auto-file-renaming=false \
                --dir="_tmp" \
                --out="$FILENAME" \
                "$URL" 2>&1) && SUCCESS=true && break
              
              echo "Fallback to curl..."
              ERROR_LOG=$(curl -L -f -# \
                --retry 3 \
                --retry-delay 5 \
                --connect-timeout 30 \
                --max-time 3600 \
                -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
                -o "_tmp/$FILENAME" \
                "$URL" 2>&1) && SUCCESS=true && break
              
              echo "Waiting ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
              RETRY_DELAY=$((RETRY_DELAY * 2))
            done
            
            if [ "$SUCCESS" = true ] && [ -f "_tmp/$FILENAME" ]; then
              if [ -s "_tmp/$FILENAME" ]; then
                echo "OK: $FILENAME"
                return 0
              else
                rm -f "_tmp/$FILENAME"
                return 1
              fi
            else
              return 1
            fi
          }
          # Process URLs
          for URL in $URLS; do
            FILENAME=$(basename "$URL" | sed 's/%20/ /g' | sed 's/%2F/-/g' | sed 's/[?#].*//')
            FILENAME=$(echo "$FILENAME" | sed 's/[<>:"|?*]/_/g')
            echo "Fetching: $URL -> $FILENAME"
            download_with_retry "$URL" "$FILENAME" || echo "Failed: $FILENAME"
          done
          if [ "$MODE" = "zip" ]; then
            if [ -n "$(ls -A _tmp 2>/dev/null)" ]; then
              RAND_ARCHIVE=$(get_random_id)
              RAND_FOLDER=$(get_unique_folder "_data" "pkg")
              mkdir -p "_data/$RAND_FOLDER"
              
              echo "Zipping into: $RAND_FOLDER/$RAND_ARCHIVE.zip"
              
              if [ -n "$ZIP_PASSWORD" ]; then
                zip -j -P "$ZIP_PASSWORD" "_data/$RAND_FOLDER/${RAND_ARCHIVE}.zip" _tmp/*
              else
                zip -j "_data/$RAND_FOLDER/${RAND_ARCHIVE}.zip" _tmp/*
              fi
              
              rm -rf _tmp
            fi
          else
            for FILE in _tmp/*; do
              [ -f "$FILE" ] || continue
              SIZE=$(stat -c%s "$FILE")
              BASENAME=$(basename "$FILE")
              BASENAME_NO_EXT="${BASENAME%.*}"
              RAND_FOLDER=$(get_unique_folder "_data" "item")
              mkdir -p "_data/$RAND_FOLDER"
              
              if [ "$SIZE" -gt "$SPLIT_THRESHOLD_BYTES" ]; then
                echo "Splitting large file: $BASENAME"
                if [ -n "$ZIP_PASSWORD" ]; then
                  zip -0 -s "${SPLIT_THRESHOLD_MB}m" -P "$ZIP_PASSWORD" "_data/$RAND_FOLDER/${RAND_FOLDER}.zip" -j "$FILE"
                else
                  zip -0 -s "${SPLIT_THRESHOLD_MB}m" "_data/$RAND_FOLDER/${RAND_FOLDER}.zip" -j "$FILE"
                fi
              else
                echo "Zipping: $RAND_FOLDER"
                if [ -n "$ZIP_PASSWORD" ]; then
                  zip -j -P "$ZIP_PASSWORD" "_data/$RAND_FOLDER/${RAND_FOLDER}.zip" "$FILE"
                else
                  zip -j "_data/$RAND_FOLDER/${RAND_FOLDER}.zip" "$FILE"
                fi
              fi
            done
            rm -rf _tmp
          fi
      - name: Upload to ds_tf
        run: |
          FTP_KEY="${{ github.event.inputs.ftp_key_text }}"
          
          # Parse credentials
          IFS='|' read -r HOST USER PASS PORT <<< "$FTP_KEY"
          
          echo "Connecting to: $HOST:$PORT"
          echo "User: $USER"
          
          
          lftp -c "
            set ftp:ssl-allow no;
            set ftp:ssl-force no;
            set net:max-retries 3;
            set net:timeout 30;
            open -u $USER,$PASS $HOST $PORT
            mirror -R --parallel=5 --delete _data /
          "
          
          echo "ds_tf Upload Completed."
          
          # Cleanup local data after upload
          rm -rf _data
